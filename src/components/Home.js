import React from "react";
import { Header } from "semantic-ui-react";

const Home = () => {
    return (
        <div>
            <Header as={'h1'}>Vinethio</Header>
            <span>Welcome</span>
        </div>
    );
};

export default Home;